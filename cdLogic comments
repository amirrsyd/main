//Comments for cdLogic
This is a basic skeleton program for cdLogic.
We will be accessing the created Task Class by Wei Ming

Commands: ADD, DELETE,LIST,EMPTY,SEARCH,COMPLETE,EDIT,INVALID,EXIT,CHANGEDIR,UNDO

3 types of tasks: FLOAT(no starttime/endtime), EVENT(timingsprovided), DATELINE(timing provided will be assumed as deadline timing)

User command is accessed and tasks will be performed by parsing user commands for each command.

Time and Dates for each task follow a standard and will be returned in that format to standardize thing. (Extract time and dates and return them in specific format)
